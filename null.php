<?php

    $name=null;
    var_dump($name); //var_dump() function is used to display the data type and value of the variable.

    echo "<br>";
    echo gettype($name); // gettype() function is used to display the data type of the variable.

    echo "<br>";
     var_dump(is_null($name));  // is_null() function is used to check whether the variable is null or not.
     ?>